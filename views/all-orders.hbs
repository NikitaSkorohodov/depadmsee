<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    .order-item {
      border: 1px solid #ccc;
      padding: 10px;
      margin-bottom: 10px;
      position: relative;
      width: 70%; /* Set the width of the order item */
      margin: 0 auto; /* Center the order item */
    }

    .course-item {
      margin-top: 5px;
      margin-bottom: 5px;
    }

    .delete-button {
      position: absolute;
      top: 10px;
      right: 10px;
      padding: 5px 10px;
      background-color: red;
      color: white;
      border: none;
      cursor: pointer;
    }

    .container {
      width: 80%;
      margin-bottom: 100px !important;
      z-index: 0;
      position: relative;
      margin: 0 auto; /* Center the container */
    }

    .first-section {
      display: flex; /* Enables Flexbox */
      flex-direction: column; /* Stack items vertically */
      justify-content: center; /* Centers content vertically */
      align-items: center; /* Centers content horizontally */
      height: 60vh; /* Makes the section take the full height of the viewport */
      padding: 20px;
      background-image: url("/image.png");
      background-size: cover; /* Cover the entire div */
      background-position: center; /* Center the background image */
      background-repeat: no-repeat; /* Prevent the image from repeating */
      width: 100%; /* Optional, set width for your div */
    }

    h1 {
      color: #EFAD12;
      font-weight: bold;
      font-size: 80px;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="first-section">
        <h1>{{__ 'All orders'}}</h1>
    </div>

    {{#if orders.length}}
      <ul>
        {{#each orders}}
          <li class="order-item">
            <form action="/orders/delete/{{_id}}" method="POST">
              <button type="submit" class="delete-button">X</button>
            </form>
            <p>Order created: {{date}}</p>
            <p>User: {{user.username}}</p>
            <p>Email: {{user.email}}</p>
            <p>Total price: {{totalPrice}} EUR</p>
            <p>Pickup point: {{deliveryPoint}}</p>
            <ul class="course-list">
              {{#each products}}
                <li class="course-item">
                  <p>Product: {{this.title}}</p>
                  <p>Price: {{this.price}} EUR</p>
                </li>
              {{/each}}
            </ul>
          </li>
        {{/each}}
      </ul>
    {{else}}
      <p>You don't have any orders yet.</p>
    {{/if}}
  </div>
</body>
</html>
